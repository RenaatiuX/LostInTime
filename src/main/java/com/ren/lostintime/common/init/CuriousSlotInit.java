package com.ren.lostintime.common.init;

public class CuriousSlotInit {

    // if u have such a name which will be used very often inside the code, then always use a static variable somewhere
    //makes it easier to change it just in case
    public static final String BELT_SLOT = "belt";
}
